# CNN-Specific Considerations

CNNs fit this problem because they preserve spatial structure, use local receptive fields, share weights, and learn hierarchical visual features from edges to full character shapes.
